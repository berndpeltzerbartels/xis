class XISSet extends XISCollection {
    /**
        * 
        * @param {Array} arr 
        */
    constructor(arr) {
        super(arr);
        this.className = 'XISSet';
    }

    add(obj) {
        if (indexOf(this.arr, obk)) {
            this.arr.push(obj);
        }
    }
}